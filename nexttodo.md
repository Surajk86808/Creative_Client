## Next Todo

- Align root docs with the moved `Leads/backend` structure and remove stale references.
- Decide whether generated `Leads/backend/output/` artifacts should stay in the repo or move to ignored/local-only storage.
- Finish cleaning remaining `website-builder/**/node_modules` folders if a fully stripped workspace is required.
- Add `NVIDIA_API_KEY` mention to any remaining operator docs that still only mention `GROQ_API_KEY`.
- Review `Mcp/` prompts and tool descriptions so they match the current backend workflow exactly.
- Consider restoring a backend `README.md` and `PIPELINE.md` summary if operators still expect those files in `Leads/backend/`.
- Add one lightweight health-check doc for "what must exist before a live run" across backend, MCP, and dashboard.
